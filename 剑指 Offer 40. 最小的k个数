class Solution {
public:
    vector<int> res;
    vector<int> getLeastNumbers(vector<int>& arr, int k) {
        if(arr.empty()||k==0)
            return {};
        // vector<int> res;
        // priority_queue<int> max_heap;
        // for(int i=0;i<k;i++)
        // max_heap.push(arr[i]);
        // for(int i=k;i<arr.size();i++){
        //     if(arr[i]<max_heap.top()){
        //         max_heap.pop();
        //         max_heap.push(arr[i]);
        //     }
        // }
        // for(int i=0;i<k;i++){
        //     res.push_back(max_heap.top());
        //     max_heap.pop();
        // }
        // return res;
    return quickSeletion<vector<int>& arr,int left,int right, 

    }
};
